# Supabase (Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Admin login (any string; this is the only admin credential)
ADMIN_PASSWORD=

# Bank details and the WhatsApp number are NOT set here — they are edited in
# Admin -> Payment, so they can be changed without a redeploy.
